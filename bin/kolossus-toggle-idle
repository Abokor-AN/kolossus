#!/bin/bash

if pgrep -x swayidle >/dev/null; then
  pkill -x swayidle
  notify-send "Kolossus" "Verrouillage automatique désactivé"
else
  kolossus-idle >/dev/null 2>&1 &
  notify-send "Kolossus" "Verrouillage automatique activé"
fi

